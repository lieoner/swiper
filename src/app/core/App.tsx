import React from 'react';

import {StatusBar, View} from 'react-native';

const App = () => {
    return (
        <View>
            <StatusBar barStyle={'light-content'} />
        </View>
    );
};

export default App;
